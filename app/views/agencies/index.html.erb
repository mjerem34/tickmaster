<% if Right.where(name: "view_index_agencies").pluck(current_user.tech.name).join('')  == 'true' %>
<% @title = "Liste de toutes les agences" %>




<div class="table-responsive">

  <table class="table table-hover">
    <thead class="header">
      <tr>
        <th>#</th>
        <th>Nom</th>
        <th>Adresse</th>
        <th>Adresse 2</th>
        <th>Adresse 3</th>
        <th>CP</th>
        <th>Ville</th>
        <th>Département</th>
        <th>Adresse email</th>
        <th>Téléphone</th>
        <th>Fax</th>
        <th>IP</th>
      </tr>
    </thead>

    <tbody>
      <% @agencies.each do |agency| %>
      <tr>
        <td onClick="document.location.href='<%=agency_path(agency)%>'"><%= agency.id == nil ? "Inconnu" : agency.id%></td>
        <td onClick="document.location.href='<%=agency_path(agency)%>'"><%= agency.name == nil ? "Inconnu" : agency.name %></td>
        <td onClick="document.location.href='<%=agency_path(agency)%>'"><%= agency.adress == nil ? "Inconnu" : agency.adress%></td>
        <td onClick="document.location.href='<%=agency_path(agency)%>'"><%= agency.adress2 == nil ? "Inconnu" : agency.adress2%></td>
        <td onClick="document.location.href='<%=agency_path(agency)%>'"><%= agency.adress3 == nil ? "Inconnu" : agency.adress3%></td>
        <td onClick="document.location.href='<%=agency_path(agency)%>'"><%= agency.cp == nil ? "Inconnu" : agency.cp %>
        <td onClick="document.location.href='<%=agency_path(agency)%>'"><%= agency.city == nil ? "Inconnu" : agency.city %></td>
        <td onClick="document.location.href='<%=agency_path(agency)%>'"><%= agency.department == nil ? "Inconnu" : agency.department %></td>
        <td onClick="document.location.href='<%=agency_path(agency)%>'"><%= agency.email == nil ? "Inconnu" : agency.email %>
        <td onClick="document.location.href='<%=agency_path(agency)%>'"><%= agency.tel == nil ? "Inconnu" : agency.tel %></td>
        <td onClick="document.location.href='<%=agency_path(agency)%>'"><%= agency.fax == nil ? "Inconnu" : agency.fax %></td>
        <td onClick="document.location.href='<%=agency_path(agency)%>'"><%= agency.ip_adress == nil ? "Inconnu" : agency.ip_adress %>
      </tr>
      <% end %>
        </tbody>
      </table>
    </div>

      <br>

      <%else%>
      <% @title = "Accès non autorisé"%>
      <p class="alert_non_authorized">
        Accès non autorisé ! <%= render 'sessions/new' %>
      </p>
      <% end %>
